type Card = {
    checkedPhrases: boolean[], 
    id: string, 
    cardId: string, 
    ownerId: string[], 
    backgroundColor: string, 
    textColor: string, 
    tilesColor: string
  }

export default Card
